!(function($){
	"use strict";

	jQuery(document).ready(function($) {

	});

	jQuery(window).on('resize', function() {
   // AwakenurSubmenuAuto();
	});

	jQuery(window).on('scroll', function() {

	});

})(jQuery);
